# 单表CRUD代码生成专业提示词集

## 概述

本文档提供了基于 `@开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md` 的各种使用场景的专业AI提示词，确保生成高质量、规范化的单表CRUD功能代码。

---

## 场景一：基于业务需求的完整CRUD生成

### 提示词模板

```
请严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 生成完整的单表CRUD功能模块：

**业务需求：**
{详细描述业务需求，包括功能目标、数据字段、业务规则等}

**模块信息：**
- 模块名称：{模块名称，如：用户信息管理}
- 表名前缀：{表名前缀，如：sys_user}
- 功能描述：{功能描述}

**生成要求：**
1. 严格遵循文档中的第六部分"完整流程AI提示词模板"
2. 按照标准顺序输出：数据表结构 → 字典数据配置 → 后端代码 → 前端代码 → 系统集成
3. 确保所有代码符合现有规范，可直接运行使用
4. 包含完整的注释和验证规则
5. 自动生成菜单配置和权限授权SQL

**输出格式：**
请按照文档规定的标准格式输出，每个部分用明确的标题分隔。

开始生成完整的CRUD功能模块。
```

### 使用示例

```
请严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 生成完整的单表CRUD功能模块：

**业务需求：**
需要开发一个商品分类管理功能，用于管理电商平台的商品分类信息。包括分类名称、分类编码、排序号、状态（启用/禁用）、上级分类、分类图标、分类描述等字段。支持树形结构显示，具备完整的增删改查功能。

**模块信息：**
- 模块名称：商品分类管理
- 表名前缀：product_category
- 功能描述：商品分类信息的维护和管理

**生成要求：**
1. 严格遵循文档中的第六部分"完整流程AI提示词模板"
2. 按照标准顺序输出：数据表结构 → 字典数据配置 → 后端代码 → 前端代码 → 系统集成
3. 确保所有代码符合现有规范，可直接运行使用
4. 包含完整的注释和验证规则
5. 自动生成菜单配置和权限授权SQL

**输出格式：**
请按照文档规定的标准格式输出，每个部分用明确的标题分隔。

开始生成完整的CRUD功能模块。
```

---

## 场景二：基于原型图的CRUD生成

### 提示词模板

```
请根据提供的原型图，严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 生成单表CRUD功能：

**原型图分析要求：**
1. 分析原型图中的表单字段，生成对应的数据库字段
2. 根据控件类型（输入框、下拉框、日期选择器等）推断数据库字段类型
3. 识别查询条件区域，设计相应的索引字段
4. 分析表格列的显示顺序和格式要求
5. 识别下拉选择、单选框等控件，生成对应的字典配置

**模块信息：**
- 模块名称：{从原型图中识别的功能名称}
- 功能描述：{根据原型图分析的功能描述}

**生成要求：**
1. 首先说明从原型图中识别到的关键信息
2. 严格按照文档第一部分的"原型图分析要素"进行分析
3. 生成完整的数据表结构、后端代码、前端代码
4. 前端页面布局要尽可能贴近原型图设计
5. 确保字典字段正确使用j-dict-select-tag组件

**输出格式：**
### 原型图分析结果
### 数据表结构
### 字典数据配置
### 后端代码
### 前端代码
### 系统集成

请开始分析原型图并生成代码。
```

---

## 场景三：数据表结构设计专用

### 提示词模板

```
请根据以下需求，严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 第一部分规范设计数据表结构：

**需求描述：**
{详细的业务需求描述}

**设计要求：**
1. 严格按照文档第1.1节"AI提示词模板"执行
2. 表名使用 {模块名}_{实体名} 格式
3. 必须包含标准字段：id, del_flag, create_by, create_time, update_by, update_time
4. 字段类型严格按照规范选择
5. 为查询、排序、分组字段创建索引
6. 使用utf8mb4字符集，InnoDB引擎
7. 每个字段必须有清晰的中文注释

**输出要求：**
- 完整的CREATE TABLE语句
- 如有枚举字段，同时生成对应的字典配置SQL
- 说明表结构设计的考虑因素

请开始设计数据表结构。
```

---

## 场景四：后端代码生成专用

### 提示词模板

```
请根据以下数据表结构，严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 第三部分规范生成后端代码：

**表结构SQL：**
{完整的CREATE TABLE语句}

**模块信息：**
- 模块名（驼峰）：{ModuleName}
- 模块名（小写）：{moduleNameLowerCase}
- 表名：{table_name}
- 功能描述：{module_description}

**生成要求：**
1. 严格按照文档第3.2节"AI提示词模板"执行
2. 生成完整的四层架构代码：Entity、Mapper、Service、Controller
3. 实体类必须包含所有标准注解：@Dict、@Excel、@JsonFormat等
4. Controller必须包含标准CRUD接口
5. 所有接口使用Result包装返回结果
6. 权限注解格式：{moduleNameLowerCase}:{table_name}:操作
7. 包路径：org.jeecg.modules.{moduleNameCamelCase}

**输出顺序：**
1. Entity实体类
2. Mapper接口和XML
3. Service接口和实现类
4. Controller控制器

请开始生成后端代码。
```

---

## 场景五：前端代码生成专用

### 提示词模板

```
请根据以下后端实体类，严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 第四部分规范生成前端代码：

**实体类信息：**
{完整的Entity类代码}

**模块信息：**
- 模块名（PascalCase）：{ModuleName}
- 模块名（kebab-case）：{module-name-kebab-case}
- 模块名（小写）：{moduleNameLowerCase}
- 功能描述：{module_description}

**生成要求：**
1. 严格按照文档第4.2节"AI提示词模板"执行
2. 生成完整的前端页面：List.vue、Modal.vue、Form.vue
3. 使用JeecgListMixin实现标准CRUD功能
4. 表格列配置要包含所有业务字段
5. 表单组件要包含所有字段的输入控件
6. 字典字段使用j-dict-select-tag组件
7. 日期字段使用a-date-picker组件
8. 权限控制使用v-has指令
9. 代码放置在 src/views/{moduleNameKebabCase}/ 目录下

**输出顺序：**
1. 列表页面（List.vue）
2. 弹窗组件（Modal.vue）
3. 表单组件（Form.vue）

请开始生成前端代码。
```

---

## 场景六：菜单配置SQL生成专用

### 提示词模板

```
请严格按照 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 第5.1节规范生成菜单配置SQL：

**模块信息：**
- 模块描述：{module_description}
- 模块名（PascalCase）：{ModuleName}
- 模块名（kebab-case）：{moduleNameKebabCase}
- 模块名（小写）：{moduleNameLowerCase}
- 表名：{table_name}

**生成要求：**
1. 严格按照文档第5.1.6节"完整的菜单配置和权限授权SQL整合模板"
2. parent_id设置为NULL（一级菜单）
3. component路径格式：{moduleNameKebabCase}/{ModuleName}List
4. url路径格式：/{moduleNameKebabCase}/{moduleNameKebabCase}List
5. 包含所有标准权限按钮：add、edit、delete、deleteBatch、exportXls、importExcel
6. 自动生成admin角色权限授权SQL
7. 使用时间戳+随机数生成唯一ID

**输出要求：**
- 完整的菜单配置SQL
- 完整的权限按钮SQL
- admin角色授权SQL
- 整合为单一可执行脚本

请开始生成菜单配置SQL。
```

---

## 场景七：快速生成（简化版）

### 提示词模板

```
基于 @开发规范/09-模板文件/代码生成模板/管理后台单表代码生成规范.md 快速生成：

**需求：** {简要需求描述}
**模块：** {模块名称}
**表名：** {table_name}

生成完整的单表CRUD功能，包括：数据表、后端代码、前端代码、菜单配置。
```

---

## 使用指南

### 1. 选择合适的场景
根据您的具体需求选择对应的提示词：
- **完整开发**：使用场景一
- **有原型图**：使用场景二
- **只需表结构**：使用场景三
- **只需后端**：使用场景四
- **只需前端**：使用场景五
- **只需菜单**：使用场景六
- **快速生成**：使用场景七

### 2. 替换占位符
将提示词中的占位符替换为实际值：
- `{模块名称}`：实际的模块名称
- `{table_name}`：数据库表名
- `{详细需求描述}`：具体的业务需求
- 等等

### 3. 质量检查
生成代码后，参考文档第七部分"质量控制与最佳实践"进行检查。

### 4. 部署测试
按照文档第八部分"部署与测试指南"进行部署和测试。

---

## 注意事项

1. **严格遵循模板**：所有提示词都要求严格按照规范文档执行
2. **完整性检查**：确保生成的代码包含所有必要的组件
3. **规范性验证**：生成后要检查是否符合项目编码规范
4. **功能测试**：部署后要进行完整的功能测试
5. **文档更新**：如有特殊需求，及时更新提示词模板
